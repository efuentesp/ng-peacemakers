.modal-header
  h3
    | Crear Escuela

.modal-body
  form.form-horizontal(name="createForm", novalidate=true)
    .control-group
      label.control-label(for="name")
        | Nombre
      .controls
        input.input-xlarge(type="text", id="name", ng-model="school.name", required=true, placeholder="Nombre de la Escuela")
    .control-group
      label.control-label(for="www")
        | www
      .controls
        input.input-xlarge(type="url", id="www", ng-model="school.www", placeholder="Dirección de Internet")

.modal-footer
  button.btn(ng-click="cancel()")
    | Cerrar
  button.btn.btn-primary(ng-click="submit()", ng-disabled="createForm.$invalid")
    i.icon-ok.icon-white
    |  Guardar